import React from 'react';
import Button from './button';

const Tester: React.FC = () => (
  <div>
    <Button variation="success">comprar</Button>
    <Button variation="primary">comprar</Button>
    <Button variation="secondary">comprar</Button>
    <Button variation="danger">comprar</Button>
    <Button variation="warning">comprar</Button>
  </div>
);

export default Tester;
